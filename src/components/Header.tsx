import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ backgroundColor: '#1e293b', padding: '1.5rem 2rem', color: '#f8fafc', textAlign: 'center' }}>
      <h2>Mi App</h2>
    </header>
  );
};

// Esta es la línea que falta o está mal escrita en tu archivo
export default Header;